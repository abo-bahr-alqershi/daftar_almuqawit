# دليل النسخ الاحتياطي السحابي

## 📍 الوضع الحالي

حاليًا، التطبيق يستخدم **Firebase Storage** للنسخ الاحتياطي السحابي، وليس **Google Drive**.

### الفرق بين Firebase Storage و Google Drive:

| الميزة | Firebase Storage | Google Drive |
|--------|-----------------|--------------|
| **النوع** | تخزين سحابي للتطبيقات | تخزين سحابي شخصي |
| **الوصول** | عبر Firebase Console فقط | عبر drive.google.com |
| **التكامل** | سهل مع Flutter | يحتاج مكتبات إضافية |
| **التكلفة** | مجاني حتى 5GB | مجاني حتى 15GB |
| **الظهور للمستخدم** | لا يظهر في Google Drive | يظهر مباشرة في Drive |

---

## 🔍 كيفية الوصول للنسخ الاحتياطية الحالية (Firebase Storage)

### الطريقة 1: عبر Firebase Console

1. افتح [Firebase Console](https://console.firebase.google.com)
2. اختر المشروع: `daftar_almuqawit`
3. من القائمة الجانبية → **Storage**
4. ستجد مجلد `backups/` يحتوي على النسخ الاحتياطية
5. كل مستخدم لديه مجلد فرعي: `backups/{userId}/`

### الطريقة 2: عبر التطبيق (يجب إضافتها)

يمكن إضافة شاشة لعرض وإدارة النسخ السحابية:
- عرض قائمة النسخ الاحتياطية
- تحميل نسخة معينة
- حذف النسخ القديمة

---

## 🚀 الخيارات المتاحة

### الخيار 1: الاستمرار مع Firebase Storage (موصى به)

**المميزات:**
✅ الكود جاهز ويعمل  
✅ أسرع وأكثر أمانًا  
✅ لا يحتاج أذونات Google Drive  
✅ مجاني تمامًا للاستخدام العادي  

**المطلوب:**
- إضافة شاشة لعرض النسخ السحابية في التطبيق
- تفعيل المصادقة (Firebase Auth)

---

### الخيار 2: التحويل إلى Google Drive

**المميزات:**
✅ يظهر للمستخدم في drive.google.com  
✅ المستخدم يستطيع إدارة الملفات بنفسه  

**العيوب:**
❌ يحتاج إعداد Google Drive API  
❌ يحتاج مكتبة إضافية (`googleapis` package)  
❌ أكثر تعقيدًا في الإعداد  
❌ يحتاج موافقة المستخدم على الأذونات  

**المطلوب للتحويل:**
1. إضافة `googleapis: ^11.0.0` و `google_sign_in: ^6.1.5`
2. إعداد Google Cloud Console
3. تفعيل Drive API
4. كتابة كود جديد للرفع/التحميل

---

### الخيار 3: دعم كلا الخيارين

إعطاء المستخدم خيار الرفع إلى:
- Firebase Storage (افتراضي)
- Google Drive (اختياري)

---

## 💡 التوصية

**أنصح بالخيار 1 (Firebase Storage)** للأسباب التالية:

1. **الكود جاهز**: جميع الوظائف مبرمجة ومختبرة
2. **الأمان**: بيانات المستخدم محمية ولا تظهر لأي شخص آخر
3. **السهولة**: لا يحتاج إعدادات معقدة
4. **المرونة**: يمكن إضافة واجهة داخل التطبيق لإدارة النسخ

### ما يجب إضافته فقط:

1. **شاشة إدارة النسخ السحابية** في التطبيق:
   ```
   الإعدادات > النسخ الاحتياطي > النسخ السحابية
   ```
   - عرض قائمة النسخ المتاحة
   - تاريخ كل نسخة وحجمها
   - زر تحميل/استعادة
   - زر حذف

2. **تفعيل المصادقة**: التأكد من تسجيل دخول المستخدم

---

## 📋 خطوات سريعة لتفعيل Firebase Storage

### 1. التحقق من إعدادات Firebase

تأكد من وجود ملف `google-services.json` في:
```
android/app/google-services.json
```

### 2. التحقق من قواعد Firebase Storage

في Firebase Console → Storage → Rules:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /backups/{userId}/{allPaths=**} {
      // المستخدم يستطيع القراءة والكتابة في مجلده فقط
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 3. تفعيل المصادقة في التطبيق

تأكد من تسجيل دخول المستخدم قبل النسخ الاحتياطي:

```dart
// في BackupScreen أو حيث تريد
final user = FirebaseAuth.instance.currentUser;
if (user == null) {
  // عرض رسالة: "يجب تسجيل الدخول أولاً"
  // أو توجيه المستخدم لشاشة تسجيل الدخول
}
```

---

## 🧪 اختبار النسخ السحابي

### الخطوات:

1. **تسجيل الدخول** في التطبيق (Firebase Auth)
2. **إنشاء نسخة احتياطية** من شاشة النسخ الاحتياطي
3. **فحص السجلات** (Logs):
   ```
   🔄 بدء رفع النسخة للسحابة...
   📤 تقدم الرفع: 100%
   ✅ تم رفع النسخة للسحابة بنجاح!
   ```
4. **التحقق من Firebase Console**:
   - افتح Firebase Console → Storage
   - تحقق من وجود الملف في `backups/{userId}/`

### إذا ظهرت رسالة:
```
⚠️ لم يتم تسجيل الدخول - لن يتم رفع النسخة للسحابة
```

**الحل:** تفعيل المصادقة أولاً.

---

## ❓ ماذا تريد أن تفعل؟

### الخيار A: إضافة شاشة النسخ السحابية (موصى به)
سأقوم بإنشاء شاشة جديدة داخل التطبيق لعرض وإدارة النسخ الاحتياطية من Firebase Storage.

### الخيار B: التحويل إلى Google Drive
سأقوم بإعادة كتابة الكود للرفع إلى Google Drive بدلاً من Firebase Storage.

### الخيار C: فقط شرح كيفية الوصول للنسخ الحالية
سأعطيك دليل مفصل للوصول للنسخ في Firebase Console.

---

**اختر الخيار الذي تفضله وسأنفذه فوراً! 🚀**
