# 🔧 حل مشكلة Firebase Duplicate App - دليل يدوي

## ❌ المشكلة
```
[core/duplicate-app] A Firebase App named "[DEFAULT]" already exists
```

## 🎯 الحل النهائي (خطوات يدوية)

### الخطوة 1: احذف التطبيق من الجهاز يدوياً

**على الجهاز:**
1. افتح **الإعدادات** (Settings)
2. **التطبيقات** (Apps)
3. ابحث عن **دفتر المقوت** (daftar_almuqawit)
4. اضغط **إلغاء التثبيت** (Uninstall)
5. تأكيد الحذف

### الخطوة 2: أعد تشغيل الجهاز (اختياري لكن مفضل)

```bash
adb reboot
```

**أو يدوياً:** اضغط زر الطاقة → إعادة التشغيل

### الخطوة 3: تنظيف المشروع

```bash
cd /home/ameen/Desktop/daftar_almuqawit
flutter clean
rm -rf build/
flutter pub get
```

### الخطوة 4: بناء APK جديد

```bash
flutter build apk --debug
```

### الخطوة 5: تثبيت يدوي

```bash
adb install build/app/outputs/flutter-apk/app-debug.apk
```

### الخطوة 6: تشغيل التطبيق

**من الجهاز:** افتح التطبيق من قائمة التطبيقات

**أو من Terminal:**
```bash
adb shell am start -n com.arma.daftar_almuqawit/.MainActivity
```

---

## ✅ تحقق من النجاح

عند فتح التطبيق، يجب أن:
- ✅ يفتح بدون أخطاء
- ✅ لا تظهر رسالة `duplicate-app`
- ✅ الشاشة الرئيسية تظهر بشكل طبيعي

---

## 📋 الملفات المُعدّلة للحل

| الملف | التعديل |
|-------|---------|
| `lib/main.dart` | إضافة `if (Firebase.apps.isEmpty)` قبل initializeApp |
| `lib/core/di/modules/firebase_module.dart` | حذف `sl<FirestoreService>()` |

---

## 🔍 التشخيص إذا استمرت المشكلة

### تحقق من LogCat:

```bash
adb logcat | grep -i "firebase\|duplicate"
```

### ابحث عن:
```
✅ جيد: لا توجد رسائل duplicate-app
❌ سيء: تظهر رسائل duplicate-app
```

### إذا ظهر الخطأ مرة أخرى:

تحقق من هذه الملفات:
```bash
# 1. تحقق من main.dart
grep -A 5 "Firebase.initializeApp" lib/main.dart

# يجب أن يظهر:
# if (Firebase.apps.isEmpty) {
#   await Firebase.initializeApp(
```

```bash
# 2. تحقق من firebase_module.dart  
grep "FirestoreService" lib/core/di/modules/firebase_module.dart

# يجب ألا يظهر: sl<FirestoreService>()
```

---

## 🚀 البديل: flutter run مباشرة

إذا نجح الحذف اليدوي:

```bash
flutter run --no-hot
```

الخيار `--no-hot` يمنع Hot Reload ويجبر على بناء كامل.

---

**التاريخ:** 2025-11-11  
**الحالة:** جاهز للتنفيذ اليدوي  
**المدة المتوقعة:** 5 دقائق
