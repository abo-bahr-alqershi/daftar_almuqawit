# تقرير تحليل آلية التزامن والنسخ الاحتياطي

## تاريخ التحليل: 2025-11-11

---

## 1. ملخص التحليل

تم إجراء تحليل شامل ودقيق لآلية التزامن والنسخ الاحتياطي في تطبيق دفتر المقوت. التحليل شمل:
- آلية التزامن التلقائي مع السحابة
- آلية النسخ الاحتياطي التلقائي
- آلية الاستعادة من النسخ الاحتياطية
- ربط الإعدادات بالوظائف الفعلية

---

## 2. البنية المعمارية الحالية

### 2.1 طبقات التزامن

```
┌─────────────────────────────────────────┐
│         Presentation Layer              │
│  (settings_screen.dart, sync_bloc.dart) │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│          Service Layer                  │
│  (sync_service.dart, sync_manager.dart) │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│         Data Layer                      │
│  (sync_repository_impl.dart)            │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│      Data Sources                       │
│  (local + remote datasources)           │
└─────────────────────────────────────────┘
```

### 2.2 مكونات التزامن الموجودة

✅ **موجود ويعمل:**
- `SyncService`: خدمة التزامن الأساسية
- `SyncManager`: مدير التزامن
- `SyncQueue`: قائمة انتظار العمليات
- `OfflineQueue`: قائمة العمليات غير المتصلة
- `ConflictResolver`: محلل التعارضات
- `SyncLocalDataSource`: مصدر البيانات المحلي
- `SyncRemoteDataSource`: مصدر البيانات السحابي

---

## 3. المشاكل المكتشفة

### 🔴 **المشكلة #1: التزامن التلقائي غير مرتبط بالإعدادات**

**الوصف:**
- في `settings_screen.dart:116` يوجد مفتاح تبديل (Switch) للمزامنة التلقائية
- المتغير `_autoSyncEnabled` محلي فقط داخل State
- لا يتم حفظ القيمة في SharedPreferences
- لا يوجد استدعاء لـ `SyncService.startAutoSync()` عند تفعيل الخيار

**الكود الحالي:**
```dart
// settings_screen.dart:111-120
SettingsTile.switchTile(
  title: 'المزامنة التلقائية',
  subtitle: 'مزامنة البيانات مع السحابة تلقائياً',
  icon: Icons.sync,
  iconColor: AppColors.primary,
  value: _autoSyncEnabled,
  onChanged: (value) {
    setState(() => _autoSyncEnabled = value);
  },
),
```

**التأثير:**
- تغيير الإعداد في الواجهة لا يؤثر على التزامن الفعلي
- الخيار غير وظيفي بالكامل

---

### 🔴 **المشكلة #2: عدم تفعيل التزامن التلقائي عند بدء التطبيق**

**الوصف:**
- في `main.dart` و `app.dart` لا يتم التحقق من إعداد التزامن التلقائي
- `SyncService.startAutoSync()` لا يتم استدعاؤه إطلاقاً عند بدء التطبيق
- حتى لو كان المستخدم فعّل الخيار، لن يعمل التزامن بعد إعادة فتح التطبيق

**الموقع المتوقع:**
- `AppBloc._onAppStarted()` في `app_bloc.dart:19-26`

**الكود الحالي:**
```dart
Future<void> _onAppStarted(AppStarted event, Emitter<AppState> emit) async {
  try {
    // يمكن تحميل الإعدادات المحفوظة هنا
    emit(const AppReady());
  } catch (e) {
    emit(AppError(e.toString()));
  }
}
```

**التأثير:**
- التزامن التلقائي لا يعمل أبداً حتى لو كان مفعلاً في الإعدادات

---

### 🔴 **المشكلة #3: النسخ الاحتياطي التلقائي غير مُفعّل فعلياً**

**الوصف:**
- في `backup_screen.dart:167-200` يتم حفظ إعداد `autoBackupEnabled`
- لكن لا يتم استدعاء `BackupService.scheduleAutoBackup()` عند التفعيل
- لا يتم إلغاء الجدولة عند التعطيل

**الكود الحالي:**
```dart
// backup_screen.dart:167-200
Future<void> _toggleAutoBackup() async {
  setState(() => _isAutoBackupEnabled = !_isAutoBackupEnabled);

  try {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setBool(StorageKeys.autoBackupEnabled, _isAutoBackupEnabled);
    
    // ⚠️ لا يوجد استدعاء للخدمة الفعلية
  }
}
```

**التأثير:**
- الإعداد يُحفظ لكن لا يتم تطبيقه
- النسخ الاحتياطي التلقائي لا يعمل

---

### 🔴 **المشكلة #4: عمليات النسخ والاستعادة في BackupScreen لا تستخدم الخدمة الحقيقية**

**الوصف:**
- في `backup_screen.dart:68-116` عملية النسخ الاحتياطي
- يتم استخدام `Future.delayed(const Duration(seconds: 2))` بدلاً من الخدمة الفعلية
- نفس المشكلة في عملية الاستعادة (`backup_screen.dart:120-164`)

**الكود الحالي:**
```dart
// backup_screen.dart:83-84
try {
  await Future.delayed(const Duration(seconds: 2)); // ⚠️ محاكاة فقط
  // لا يوجد استدعاء للخدمة الفعلية
}
```

**التأثير:**
- النسخ الاحتياطي لا يحدث فعلياً
- الاستعادة لا تحدث فعلياً
- المستخدم يظن أن العملية تمت بنجاح لكن لم يحدث شيء

---

### 🔴 **المشكلة #5: SettingsBloc لا يدعم إعدادات التزامن والنسخ الاحتياطي**

**الوصف:**
- في `settings_bloc.dart` يوجد دعم فقط لـ:
  - تغيير اللغة (`ChangeLanguage`)
  - تغيير الثيم (`ChangeTheme`)
- لا توجد أحداث (Events) لـ:
  - تفعيل/تعطيل التزامن التلقائي
  - تفعيل/تعطيل النسخ الاحتياطي التلقائي

**التأثير:**
- لا يمكن إدارة هذه الإعدادات عبر BLoC بشكل صحيح

---

### 🟡 **المشكلة #6: عدم التحقق من إعدادات التزامن عند استعادة الاتصال**

**الوصف:**
- في `SyncService.startAutoSync():83-96` يتم المزامنة عند توفر الاتصال
- لكن لا يتم التحقق أولاً من أن المستخدم فعّل "المزامنة التلقائية"

**الكود الحالي:**
```dart
// sync_service.dart:83-96
void startAutoSync() {
  _autoSyncSubscription?.cancel();
  
  _autoSyncSubscription = _sl<ConnectivityService>()
      .onStatusChange
      .listen((online) {
    if (online && !_isSyncing) {
      _log('الاتصال متوفر - بدء المزامنة التلقائية');
      syncOnce(); // ⚠️ لا يتم التحقق من الإعداد
    }
  });
}
```

**التأثير:**
- إذا تم استدعاء `startAutoSync()` سيتم التزامن حتى لو كان المستخدم أوقفه من الإعدادات

---

## 4. مخطط تدفق البيانات الحالي vs المطلوب

### التدفق الحالي (غير صحيح):
```
المستخدم يفعّل "المزامنة التلقائية"
        ↓
يتم تغيير _autoSyncEnabled = true
        ↓
❌ لا شيء يحدث (الإعداد محلي فقط)
```

### التدفق المطلوب (الصحيح):
```
المستخدم يفعّل "المزامنة التلقائية"
        ↓
حفظ الإعداد في SharedPreferences
        ↓
استدعاء SyncBloc.add(EnableAutoSync())
        ↓
SyncBloc يستدعي SyncManager.startAuto()
        ↓
SyncService.startAutoSync() يبدأ الاستماع للاتصال
        ↓
عند توفر الاتصال → مزامنة تلقائية
```

---

## 5. الحلول المقترحة

### ✅ **الحل #1: إضافة Events للتزامن في SettingsBloc**

إضافة الأحداث التالية:
```dart
// settings_event.dart
class ToggleAutoSync extends SettingsEvent {
  final bool enabled;
  const ToggleAutoSync(this.enabled);
}

class ToggleAutoBackup extends SettingsEvent {
  final bool enabled;
  const ToggleAutoBackup(this.enabled);
}
```

### ✅ **الحل #2: ربط settings_screen مع SettingsBloc**

تعديل الكود لاستخدام BLoC بدلاً من State المحلي:
```dart
// settings_screen.dart
SettingsTile.switchTile(
  value: settingsState.autoSyncEnabled,
  onChanged: (value) {
    context.read<SettingsBloc>().add(ToggleAutoSync(value));
  },
),
```

### ✅ **الحل #3: تفعيل الخدمات عند تغيير الإعدادات**

في SettingsBloc:
```dart
Future<void> _onToggleAutoSync(
  ToggleAutoSync event,
  Emitter<SettingsState> emit,
) async {
  await _prefs.setBool(StorageKeys.autoSyncEnabled, event.enabled);
  
  if (event.enabled) {
    _syncManager.startAuto();
  } else {
    await _syncManager.stopAuto();
  }
  
  emit(state.copyWith(autoSyncEnabled: event.enabled));
}
```

### ✅ **الحل #4: تفعيل التزامن عند بدء التطبيق**

في AppBloc:
```dart
Future<void> _onAppStarted(AppStarted event, Emitter<AppState> emit) async {
  try {
    final prefs = await _prefsService.init();
    final autoSyncEnabled = _prefsService.getBool(StorageKeys.autoSyncEnabled) ?? true;
    
    if (autoSyncEnabled) {
      _syncManager.startAuto();
    }
    
    emit(const AppReady());
  } catch (e) {
    emit(AppError(e.toString()));
  }
}
```

### ✅ **الحل #5: استخدام الخدمات الفعلية في BackupScreen**

```dart
// backup_screen.dart
Future<void> _backupNow() async {
  setState(() => _isBackingUp = true);
  
  try {
    final backupService = sl<BackupService>();
    final backupPath = await backupService.createBackup();
    
    // حفظ وقت آخر نسخة
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(StorageKeys.lastBackupTime, DateTime.now().toIso8601String());
    
    setState(() {
      _lastBackupDate = DateTime.now();
      _isBackingUp = false;
    });
  } catch (e) {
    setState(() => _isBackingUp = false);
    // عرض الخطأ
  }
}
```

---

## 6. ملفات تحتاج للتعديل

| الملف | التعديلات المطلوبة | الأولوية |
|------|---------------------|----------|
| `lib/presentation/blocs/settings/settings_event.dart` | إضافة Events جديدة | 🔴 عالية |
| `lib/presentation/blocs/settings/settings_state.dart` | إضافة حقول الإعدادات | 🔴 عالية |
| `lib/presentation/blocs/settings/settings_bloc.dart` | إضافة معالجات الأحداث | 🔴 عالية |
| `lib/presentation/screens/settings/settings_screen.dart` | ربط UI مع BLoC | 🔴 عالية |
| `lib/presentation/screens/settings/backup_screen.dart` | استخدام الخدمات الفعلية | 🔴 عالية |
| `lib/presentation/blocs/app/app_bloc.dart` | تفعيل التزامن عند البدء | 🔴 عالية |
| `lib/core/services/sync/sync_service.dart` | التحقق من الإعدادات | 🟡 متوسطة |

---

## 7. خطة التنفيذ الموصى بها

### المرحلة 1: إصلاح البنية الأساسية (يوم واحد)
1. ✅ تحديث `settings_event.dart` و `settings_state.dart`
2. ✅ تحديث `settings_bloc.dart` بالمعالجات الجديدة
3. ✅ تحديث `app_bloc.dart` لتفعيل التزامن عند البدء

### المرحلة 2: ربط الواجهة (نصف يوم)
4. ✅ تحديث `settings_screen.dart` لاستخدام BLoC
5. ✅ تحديث `backup_screen.dart` لاستخدام الخدمات الحقيقية

### المرحلة 3: الاختبار والتحقق (نصف يوم)
6. ✅ اختبار التزامن التلقائي
7. ✅ اختبار النسخ الاحتياطي التلقائي
8. ✅ اختبار الاستعادة

---

## 8. اختبارات التحقق المطلوبة

### اختبار التزامن التلقائي:
- [ ] تفعيل المزامنة التلقائية من الإعدادات
- [ ] إغلاق التطبيق وإعادة فتحه
- [ ] التحقق من أن المزامنة تعمل عند توفر الاتصال
- [ ] تعطيل المزامنة التلقائية
- [ ] التحقق من توقف المزامنة التلقائية

### اختبار النسخ الاحتياطي:
- [ ] إنشاء نسخة احتياطية يدوياً
- [ ] التحقق من وجود ملف النسخة الاحتياطية
- [ ] تفعيل النسخ الاحتياطي التلقائي
- [ ] الانتظار 24 ساعة والتحقق من إنشاء نسخة جديدة

### اختبار الاستعادة:
- [ ] اختيار نسخة احتياطية موجودة
- [ ] تنفيذ عملية الاستعادة
- [ ] التحقق من استعادة جميع البيانات بشكل صحيح

---

## 9. ملاحظات أمنية

⚠️ **تحذيرات:**
- عملية الاستعادة تستبدل قاعدة البيانات بالكامل - يجب التأكد من إغلاق جميع الاتصالات
- النسخ الاحتياطية يجب أن تكون مشفرة إذا كانت تُرفع للسحابة
- يجب التحقق من صحة ملف النسخة الاحتياطية قبل الاستعادة

---

## 10. الخلاصة

| البند | الحالة |
|------|--------|
| **آلية التزامن التلقائي** | ❌ غير وظيفية - الإعداد محلي فقط |
| **آلية النسخ الاحتياطي التلقائي** | ❌ غير وظيفية - لا يتم استدعاء الخدمة |
| **آلية الاستعادة** | ❌ غير وظيفية - محاكاة فقط |
| **ربط الإعدادات بالوظائف** | ❌ غير موجود |
| **التفعيل عند بدء التطبيق** | ❌ غير موجود |

**الحكم النهائي:**
التطبيق يحتوي على **البنية التحتية الصحيحة** للتزامن والنسخ الاحتياطي، لكن **الربط بين الطبقات غير مكتمل**. الإصلاحات مطلوبة في طبقة العرض (Presentation Layer) فقط.

---

## 11. المراجع

- `lib/core/services/sync/sync_service.dart:1-150`
- `lib/core/services/sync/sync_manager.dart:1-123`
- `lib/core/services/backup_service.dart:1-59`
- `lib/presentation/screens/settings/settings_screen.dart:1-386`
- `lib/presentation/screens/settings/backup_screen.dart:1-422`
- `lib/presentation/blocs/settings/settings_bloc.dart:1-104`
- `lib/presentation/blocs/app/app_bloc.dart:1-72`

---

**تم إعداد التقرير بواسطة:** Verdent AI  
**تاريخ:** 2025-11-11  
**الإصدار:** 1.0
